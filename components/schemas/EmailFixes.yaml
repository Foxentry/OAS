type: object
nullable: true
description: >-
  List of all changes applied to the query data in order to get the corrected
  result.
properties:
  type:
    type: string
    description: Description of the fix.
    example: SYNTAX
  subtype:
    type: string
    nullable: true
    description: Subtype of the fix.
    example: AT_SIGN_DUPLICITY
  data:
    type: object
    properties:
      typeFrom:
        type: string
        description: Customer email.
        example: email
      type:
        type: string
        description: Type of the email.
        example: email
      valueFrom:
        type: string
        description: Value from.
        example: me@email.COM
      value:
        type: string
        description: Value.
        example: me@gmail.com
