content:
  application/json:
    schema:
      type: object
      properties:
        request:
          $ref: ./../../schemas/requestBodies/location/LocationLocalizationBody.yaml
      required:
        - request
