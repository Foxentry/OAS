content:
  application/json:
    schema:
      type: object
      properties:
        request:
          type: object
          allOf:
            - $ref: ./../../schemas/requests/location/LocationLocalizationBody.yaml
          required:
            - query
      required:
        - request
