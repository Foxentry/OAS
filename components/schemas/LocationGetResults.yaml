type: object
properties:
  results:
    type: array
    items:
      type: object
      properties:
        data:
          $ref: ./LocationData.yaml
      required:
        - data
required:
  - results
