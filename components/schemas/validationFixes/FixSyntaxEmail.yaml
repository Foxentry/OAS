type: object
properties:
  group:
    enum:
      - SYNTAX
    description: Group of fix.
    example: SYNTAX
  type:
    enum:
      - VALUE_PART_ADDED
      - VALUE_PART_CHANGED
      - VALUE_PART_REMOVED
    description: Type of fix.
    example: VALUE_PART_ADDED
  subtype:
    enum:
      - AT_SIGN
      - DOMAIN_TLD
      - DOMAIN
    description: Subtype of fix.
    example: AT_SIGN
  data:
    $ref: ./FixData.yaml
